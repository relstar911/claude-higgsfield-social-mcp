"""AI-Influencer-Automation: a FastMCP server that gives an agent the tools to
run AI influencers end-to-end (persona -> image -> video -> caption -> publish).

The persona niche is pure configuration, not code: the same deterministic cycle
runs identically for every persona.
"""

__version__ = "0.1.0"
